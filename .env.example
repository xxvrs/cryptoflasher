# Values below act as defaults for the web dashboard. Fields left empty in the UI fall back to these settings.

# Ethereum mainnet RPC endpoint (Infura, Alchemy, etc.)
RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID

# Private key for the sender wallet (never commit real keys!)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# ERC20 token contract address on mainnet
TOKEN_ADDRESS=0xTOKEN_CONTRACT_ADDRESS

# Recipient wallet address
RECIPIENT=0xRECIPIENT_ADDRESS

# Token amount in whole units (will be converted using token decimals)
AMOUNT=0.1

# Optional manual gas price. Gas limit is always underfunded by the app to force failures.
# GAS_PRICE_GWEI=20

# Port for the local dashboard (defaults to 3000)
# PORT=3000
